<form method="post" action="{{ path('reclamation_delete_front', {'id': reclamation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reclamation.id) }}">
    <button class="btn btn-primary d-block mt-2">Delete</button>
</form>
