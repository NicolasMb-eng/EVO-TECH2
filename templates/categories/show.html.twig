{% extends 'base-back.html.twig' %}

{% block title %}Categories{% endblock %}

{% block body %}
    <h1>  <div class="card mb-4 py-3 border-bottom-primary">
            <div class="card-body">
                 Catégorie
            </div>
        </div></h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ category.id }}</td>
            </tr>
            <tr>
                <th>NomCategorie</th>
                <td>{{ category.nomCategorie }}</td>
            </tr>
            <tr>
                <th>DescCategorie</th>
                <td>{{ category.descCategorie }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('categories_index') }}"class="btn btn-primary btn-icon-split">
                                        <span class="icon text-white-50">
                                            <i class="fas fa-arrow-right"></i>
                                        </span>
        <span class="text">   Retour à la liste des Catégories   </span></a>

    <a href="{{ path('categories_edit', {'id': category.id}) }}" class="btn btn-secondary btn-circle btn-group-lg">
        <i class="fas fa-edit"></i></a></a>

    {{ include('categories/_delete_form.html.twig') }}
{% endblock %}
