

{{ form_start(form,{'attr': {'novalidate': 'novalidate'}}) }}
<fieldset class="border bg-white p-4 my-5 ad-feature bg-gray">
    <div class="row">
        <div class="col-lg-12">
            <table>

                <tr>
                    <td>{{ form_label(form.nom,"Nom ")  }}</td>
                    <td>{{ form_widget(form.nom) }}</td>
                <tr>
                    <td>{{ form_errors(form.nom) }}</td>
                </tr>
                </tr>


                <tr>

                    <td>{{ form_label(form.description,"Description ") }}</td>
                    <td>{{ form_widget(form.description)}}</td>
                <tr>
                    <td>{{ form_errors(form.description) }}</td>
                </tr>
                </tr>


            </table>
        </div>
    </div>
</fieldset>


<button class="btn btn-success btn-icon-split"><span class="icon text-white-50">
                                            <i class="fas fa-check"></i>
                                        </span>{{ button_label|default('Ajouter Catégorie') }}</button>
{{ form_end(form) }}
