{% extends 'base-back.html.twig' %}

{% block title %}Produits{% endblock %}

{% block body %}
    <h1>  <div class="card mb-4 py-3 border-bottom-primary">
            <div class="card-body">
                Produits
            </div>
        </div></h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produit.id }}</td>
            </tr>
            <tr>
                <th>NomProduit</th>
                <td>{{ produit.nomProduit }}</td>
            </tr>
            <tr>
                <th>DescProduit</th>
                <td>{{ produit.descProduit }}</td>
            </tr>
            <tr>
                <th>PrixProduit</th>
                <td>{{ produit.prixProduit }}</td>
            </tr>
            <tr>
                <th>QuantiteProduit</th>
                <td>{{ produit.quantiteProduit }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td><img src="{{asset ('images/products/' ~ produit.image) }}"></td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('produits_index') }}" class="btn btn-primary btn-icon-split">
        <span class="icon text-white-50">
                                            <i class="fas fa-arrow-right"></i>
                                        </span>
        <span class="text">Retour à la liste des produits</span></a>


    <a href="{{ path('produits_edit', {'id': produit.id}) }}" class="btn btn-primary btn-circle btn-group-lg">
        <i class="fas fa-edit"></i></a>

    {{ include('produits/_delete_form.html.twig') }}

{% endblock %}
